apiVersion: v1
kind: Service
metadata:
  name: {{ include "yolo-chart.fullname" . }}
  labels:
    {{- include "yolo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yolo-chart.selectorLabels" . | nindent 4 }}
